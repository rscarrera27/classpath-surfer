---
name: search-classpath
description: Search for symbols, dependencies, or packages in Gradle project dependencies. Use when the user needs to find API signatures, browse dependencies, or explore packages.
allowed-tools: Agent(search-classpath)
argument-hint: "[symbol-name | dep [pattern] | pkg [pattern]]"
---

Dispatch the **search-classpath** agent with the query "$ARGUMENTS".
