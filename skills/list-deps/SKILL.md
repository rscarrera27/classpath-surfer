---
name: list-deps
description: List indexed project dependencies with symbol counts. Use when the user wants to explore or filter project dependencies.
allowed-tools: Agent(list-deps)
argument-hint: "[filter-pattern]"
---

Dispatch the **list-deps** agent with the query "$ARGUMENTS".
